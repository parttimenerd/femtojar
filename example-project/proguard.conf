# Project-specific ProGuard rules.
# The bundled femtojar default config already provides:
#   - java.base jmod as library JAR
#   - keep rules for main classes, native methods, enums, serialization, annotations
# Your application may contain more items that need to be preserved;
# typically classes that are dynamically created using Class.forName:
-keep class picocli.** { *; }