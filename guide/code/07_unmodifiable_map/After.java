import java.util.HashMap;
import java.util.Map;

public class After {
    static final Map<String, Integer> CODES = new HashMap<>();

    static {
        CODES.put("ok", 0);
        CODES.put("err", 1);
    }
}
